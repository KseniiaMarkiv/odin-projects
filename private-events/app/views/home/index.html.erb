<div class="ui container">
  <h1 class="ui header">Home page</h1>

  <div class="ui list">
    <% @users.each do |user| %>
      <div class="item">
        <div class="header"><%= user.email %></div>
        <div class="description">
          - <%= user.created_at.in_time_zone("Eastern Time (US & Canada)").strftime("%B %d, %Y, %I:%M %p") %>
        </div>
      </div>
    <% end %>
  </div>
</div>
